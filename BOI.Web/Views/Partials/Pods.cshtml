@inherits UmbracoViewPage<IPublishedContent>
@{
    var pods = Model.Value("pods", fallback: Fallback.ToDefaultValue, defaultValue: Enumerable.Empty<IPublishedContent>());

    if (pods.NullOrEmpty())
    {
        return;
    }
}
<partial name="/views/partials/podsRender.cshtml" model="pods" />
